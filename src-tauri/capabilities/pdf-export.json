{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "pdf-export",
  "description": "Minimal capability for the PDF export window — only allows triggering print",
  "windows": ["pdf-export"],
  "permissions": [
    "core:default"
  ]
}
